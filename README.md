# Patient Data App

## Overview

This is a simple web application that parses patient data from a JSON file using JavaScript and displays the information on a web page using HTML.

## Technologies Used
- HTML
- JavaScript
- JSON

## Description

The application reads structured patient data from a JSON source, processes it with JavaScript, and dynamically displays the patient information in the browser.

